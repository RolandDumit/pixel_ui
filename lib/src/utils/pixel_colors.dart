import 'package:flutter/material.dart';

class PixelColors {
  /// The default color of the shadow.
  static const Color shadowColor = Colors.black;

  /// The default color of the border.
  static const Color borderColor = Colors.black;

  /// The default color of the pixel.
  static const Color pixelColor = Color(0xFF4CAF50);

  /// The default color of the pixel when pressed.
  static const Color pressedPixelColor = Color(0xFF388E3C);

  static const Color deactivatedPixelColor = Color(0xFFBDBDBD);
}
